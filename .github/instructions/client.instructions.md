---
applyTo: "apps/client/**"
---

# Client Instructions

The client owns Phaser rendering, input capture, interpolation, local UI, and visual effects.

Do not make the client authoritative for hits, damage, currency, ownership, or persistent account state.

Coordinate network protocol changes with `packages/shared` and server validation.

