# Claude Code Instructions

Read `AGENTS.md` first, then read relevant design and architecture documents for the task.

By default, act as a reviewer unless the user explicitly asks for implementation.

Do not commit or push unless the user explicitly asks for it.

For reviews, report:

- severity;
- file references;
- impact;
- proposed fix;
- test gaps.

Do not duplicate all project rules here. `AGENTS.md` is the source of general agent policy.

